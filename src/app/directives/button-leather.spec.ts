import { ButtonLeather } from './button-leather';

describe('ButtonLeather', () => {
  it('should create an instance', () => {
    const directive = new ButtonLeather();
    expect(directive).toBeTruthy();
  });
});
