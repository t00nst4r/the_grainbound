import { ButtonSeal } from './button-seal';

describe('ButtonSeal', () => {
  it('should create an instance', () => {
    const directive = new ButtonSeal();
    expect(directive).toBeTruthy();
  });
});
